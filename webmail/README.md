# Deprecated Webmail Scaffold

This directory is not the active OpenMailStack webmail app. It is a leftover Vite starter scaffold kept only as a reference until the repository is cleaned up.

Use these directories instead:

- `webmail-frontend/`: canonical React/Vite webmail UI.
- `webmail-backend/`: Node/Express API and sync proxy.
